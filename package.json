{
  "name": "mocker-api-locally",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run start --prefix mocker-api\" \"npm run start --prefix react-app\"",
    "build": "npm run build --prefix react-app"
  },
  "devDependencies": {
    "concurrently": "^6.5.1",
    "nodemon": "^3.1.0"
  }
}
